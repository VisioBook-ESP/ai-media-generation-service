{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "ai-media-generation-service.fullname" . }}
  labels:
    {{- include "ai-media-generation-service.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "ai-media-generation-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "ai-media-generation-service.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}